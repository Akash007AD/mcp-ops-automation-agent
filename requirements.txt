# MCP SDK
mcp>=1.0.0

# LLM — Groq
groq>=0.9.0

# Slack
slack-sdk>=3.27.0

# Google (Sheets tracker + Calendar)
gspread>=6.1.0
google-auth>=2.29.0
google-api-python-client>=2.126.0

# Notion (tracker backend + standalone workspace tools)
notion-client>=2.2.1

# Telegram (httpx is the HTTP client — no heavy bot framework needed)
httpx>=0.27.0

# Scheduling
schedule>=1.2.2

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Email is now plain SMTP via stdlib smtplib — no extra package required.
# Remove these if they are still in your venv:
#   pip uninstall resend sendgrid
